apiVersion: v2
name: jforwarder
description: Helm chart for the jforwarder microservices (deployments, services, configs, postgres databases and ingress)
type: application

# Chart version. Bump on every change of the chart itself.
version: 0.1.0

# Version of the jforwarder application. Used as a default image tag
# when neither image.tag nor a per service image.tag is set.
appVersion: "1.9.6-SNAPSHOT"

keywords:
  - jforwarder
  - microservices
  - spring-boot
  - kafka

home: https://github.com/Dankoy/jforwarder
sources:
  - https://github.com/Dankoy/jforwarder
