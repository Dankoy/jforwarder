apiVersion: v1
kind: Service
metadata:
  name: telegram-chat-service-postgres-db
  namespace: jforwarder
  labels:
    app: telegram-chat-service-postgres-db
spec:
  type: NodePort
  selector:
    app: telegram-chat-service-postgres-db
  ports:
    - port: 5432
      targetPort: 5432