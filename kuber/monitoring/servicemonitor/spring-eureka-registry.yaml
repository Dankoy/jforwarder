apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spring-eureka-registry
  namespace: monitoring
  labels:
    release: kube-prometheus-stack # matches kube-prometheus-stack release label
spec:
  namespaceSelector:
    matchNames:
    - jforwarder # namespace where the service is located
  selector:
    matchLabels:
      app: spring-eureka-registry-service # service label to match
  endpoints:
  - targetPort: 8761
    path: /actuator/prometheus
    interval: 15s