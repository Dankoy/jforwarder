apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: subscriptions-holder
  namespace: monitoring
  labels:
    release: kube-prometheus-stack # matches kube-prometheus-stack release label
spec:
  namespaceSelector:
    matchNames:
    - jforwarder # namespace where the service is located
  selector:
    matchLabels:
      app: subscriptions-holder-service # service label to match
  endpoints:
  - targetPort: 8082
    path: /actuator/prometheus
    interval: 15s