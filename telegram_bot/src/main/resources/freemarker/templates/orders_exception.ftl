Expected full command "*${command} {subscription_type}*"

For example:

*${command} community*

where:
  _community_ - is the type of subscription

Or

  *${command} tag*

where:
  _tag_ - is the type of subscription

Or

  *${command} channel*

where:
  _channel_ - is the type of subscription

Full list of commands can be found in /help